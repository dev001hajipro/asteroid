{
  "name": "asteroid",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "browser-sync start -b chrome --directory --server --files '**/*.css, **/*.js, **/*.html, !node_modules/**'",
    "watch:js": "",
    "watch": "run-p serve"
  },
  "keywords": ["p5.js"],
  "author": "dev001hajipro",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.18.13",
    "npm-run-all": "^4.1.1",
    "rimraf": "^2.6.2"
  }
}
